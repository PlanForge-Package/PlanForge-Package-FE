# Prettier(endOfLine: lf) 및 .editorconfig 와 맞추기 위해 체크아웃 시에도 LF 를 유지한다.
* text=auto eol=lf

# 바이너리
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.pdf binary

# 자동 생성물은 diff 에서 접는다.
pnpm-lock.yaml linguist-generated=true -diff
